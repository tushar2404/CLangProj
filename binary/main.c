#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
   int dig,a,b,c,dig1;
   scanf("%d",&a);
   while(a!=0)
   {
       b=a%2;
       dig=log10(b);
       p
       while(b!=0)
       {
           c=b%10;
''       }
       a=a/2;
   }

    return 0;
}
